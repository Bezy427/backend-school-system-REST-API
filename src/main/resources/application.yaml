spring:
  application:
    name: school-system
  datasource:
    url: jdbc:mysql://localhost:3306/school_db?createDatabaseIfNotExist=true
    username: root
    password: Rusamu2003!!
  jpa:
    show-sql: true
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 900 #5 minutes
    refreshTokenExpiration: 604800 # 7 days

